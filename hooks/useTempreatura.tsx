import React, { useState } from 'react'

export const useTempreatura = (inicialNumber: number) => {
  const [color, setColor] = useState();
  const [numero, setNumero] = useState(inicialNumber);
  return {

  }
}
